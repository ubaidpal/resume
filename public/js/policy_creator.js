/**
 * Created by   : Nauman Munawar
 * Project Name : BlockAPT
 * Date         : 6/8/2018 12:40 PM
 */

 var app = angular.module('PolicyCreator', []).constant('url', [getDevicePolicy]);



 $(document).ready(function(){

   /*
       roi_annual_cost_it_resource = $scope.roi_annual_cost_it_resource;
    roi_it_security_size = $scope.roi_it_security_size;

    if(roi_annual_cost_it_resource > 0 && roi_it_security_size > 0)
    {
        $scope.roi_current_annual_cost_it = roi_annual_cost_it_resource * roi_it_security_size;
    }  

     if ($('#work_cost').val().length > 0 && $('#1_resource').val().length > 0) {
        $('#roi_vuln_cost').val($("#work_cost").val() / $('#1_resource').val());
    } else {
        $('#roi_vuln_cost').val('');
    }*/

});